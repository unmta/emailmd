{
  "name": "emailmd-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "packageManager": "npm@10.9.4",
  "devDependencies": {
    "turbo": "^2"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "npm -w web run dev",
    "test": "turbo test",
    "lint": "turbo lint"
  }
}
